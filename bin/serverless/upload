#! /usr/bin/bash

ABS_PATH=$(readlink -f "$0")
BIN_PATH="$(dirname "$(dirname "$ABS_PATH")")"
SERVERLESS_PATH="$BIN_PATH/serverless"
DATA_FILE="$SERVERLESS_PATH/files/data.jpg"

echo $DATA_FILE

aws s3 cp $DATA_FILE "s3://assets.$DOMAIN_NAME/avatars/original/" 